# 백엔드 환경 변수
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# 백엔드 URL (OAuth redirect URI 구성용)
BACKEND_URL=http://localhost
BACKEND_PORT=5000

# JWT
JWT_SECRET=change_this_to_a_secure_random_string
JWT_EXPIRES_IN=7d

# 데이터베이스
DB_HOST=postgres
DB_USER=todo
DB_PASS=todo123
DB_NAME=tododb

# OAuth (자동 구성: BACKEND_URL + BACKEND_PORT + /api/auth/{provider}/callback)
KAKAO_REST_API_KEY=your_kakao_rest_api_key
KAKAO_REDIRECT_URI=http://localhost:5000/api/auth/kakao/callback
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret
NAVER_REDIRECT_URI=http://localhost:5000/api/auth/naver/callback

# 프론트엔드 URL (OAuth 리다이렉트용)
FRONTEND_URL=http://localhost
FRONTEND_PORT=3000

# 기타
ALLOW_TEST_TOKEN_IN_PROD=false